// src/utils/cloudinary.js
// Stub para evitar error de importación. Implementa solo las funciones necesarias.

export async function deleteFromCloudinary(publicId, resourceType) {
  // Aquí deberías implementar la lógica real de Cloudinary si la necesitas.
  // Por ahora, solo simula éxito.
  return true;
}
